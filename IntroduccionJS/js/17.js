const numero1 = 20;
const numero2 = "20";

console.log(parseInt(numero2)); // 20 parseInt es una función que convierte un string a número
console.log(numero1.toString()); // "20" toString es un método que convierte un número a string